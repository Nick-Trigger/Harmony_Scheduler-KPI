services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: harmonyKPI
      POSTGRES_PASSWORD: harmonyKPI
      POSTGRES_DB: harmonyKPI
    ports:
      - "5432:5432"
    volumes:
      - harmonyKPI_db:/var/lib/postgresql/data

volumes:
  harmonyKPI_db: